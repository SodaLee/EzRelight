metrics:
  - type: fid
    model:
      dims: 2048
    data:
      dataset:
        resize_shape: [512, 512]
        ext: png
      batch_size: 64
      num_workers: 16

  - type: fvd
    model:
      # type: videogpt
      # path: fid_metrics/i3d_torchscript.pt
      type: styleganv
      path: fid_metrics/i3d_torchscript.pt
    data:
      dataset:
        sequence_length: 16
        resize_shape: [224, 224]
        ext: png
      batch_size: 4
      num_workers: 16

# num_iters: 1000
paths: []
